# Custom Rules for Lyrico Project

- **Commit Signing**: Always ask the user to sign commits (e.g. prompt them to enter their GPG/SSH passphrase or verify the commit signing) when committing changes. Never bypass signing using `--no-gpg-sign` unless explicitly instructed by the user.
